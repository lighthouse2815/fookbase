package com.dangngulon.frontend.core.common.errors;

public enum RegisterError {
    LAST_NAME_EMPTY,
    FIRST_NAME_EMPTY,
    EMAIL_EMPTY,
    USERNAME_EMPTY,
    PASSWORD_EMPTY,
    PASSWORD_INVALID,
    CONFIRM_PASSWORD_EMPTY,
    PASSWORD_NOT_MATCH,
    OTP_EMPTY
}
